<h3>BlindBoxCategoriesTable</h3>



@if (BlindBoxCategories == null)
{
    <p>Đang tải dữ liệu...</p>
}
else
{
    <table class="table">
        <thead>
            <tr>
                <th>
                    Name
                </th>
                <th>
                    Description
                </th>
                <th>
                    CreatedAt
                </th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in BlindBoxCategories)
            {
                <tr>
                    <td>
                        @item.Name
                    </td>
                    <td>
                        @item.Description
                    </td>
                    <td>
                        @item.CreatedAt
                    </td>
                    <td>
                        <a asp-page="./Edit" asp-route-id="@item.Id">Edit</a> |
                        <a asp-page="./Details" asp-route-id="@item.Id">Details</a> |
                        <a asp-page="./Delete" asp-route-id="@item.Id">Delete</a>
                    </td>
                </tr>
            }
        </tbody>
    </table>

}
